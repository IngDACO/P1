# Versiones fijadas a majors ESTABLES para evitar segfaults por ruedas nativas
# bleeding-edge. Pensado para Python 3.12 (fíjalo en Streamlit Cloud → Settings →
# Python version). En Python 3.14 solo hay ruedas de las últimas versiones (inestables).
streamlit>=1.39,<2   # >=1.39: clase CSS st-key-<key> por widget (colorea el tablero del roster, v169)
pypdf>=4.0.0,<7
pandas>=2.0.0,<2.3
numpy>=1.26.0,<2
openpyxl>=3.1.0,<4
reportlab>=4.0.0,<5
anthropic>=0.25.0
svglib>=1.5.0,<2
gspread>=6.0.0,<7
google-auth>=2.28.0,<3
lxml>=5.0,<6
pillow>=10.0,<12
pyarrow>=14.0,<18
# Login persistente (cookies). Import perezoso: si falla, la app sigue sin persistencia.
extra-streamlit-components>=0.1.60,<0.2
# Base de datos de zonas horarias para zoneinfo (core/clock.py, hora local por grupo, v173).
tzdata
# Mapa interactivo para fijar la ubicación de un proyecto con un pin (v193). Sin API key
# (usa OpenStreetMap). Import perezoso + fallback a coordenadas manuales si no está.
folium>=0.15,<1
streamlit-folium>=0.20,<1
# Códigos QR para el inventario de activos (v263). Pure-python, sin ruedas nativas → seguro en el Cloud.
segno>=1.6,<2
# Firma DIBUJADA a mano en el Pre-Start (v383). Es un componente de 2023, así que se
# probó antes contra el Streamlit que corre aquí (1.57): importa, renderiza, captura el
# trazo (el PNG pasa de 543 B en blanco a 5.245 B firmado) y reportlab lo acepta.
# Import PEREZOSO en `prestart_ui`: si el componente no está, el Pre-Start sigue
# funcionando con nombre e iniciales en vez de romperse.
streamlit-drawable-canvas>=0.9.3,<1
