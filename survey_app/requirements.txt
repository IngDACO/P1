# Versiones fijadas a majors ESTABLES para evitar segfaults por ruedas nativas
# bleeding-edge. Pensado para Python 3.12 (fíjalo en Streamlit Cloud → Settings →
# Python version). En Python 3.14 solo hay ruedas de las últimas versiones (inestables).
streamlit>=1.35,<2
pypdf>=4.0.0,<7
pandas>=2.0.0,<2.3
numpy>=1.26.0,<2
openpyxl>=3.1.0,<4
reportlab>=4.0.0,<5
anthropic>=0.25.0
svglib>=1.5.0,<2
gspread>=6.0.0,<7
google-auth>=2.28.0,<3
lxml>=5.0,<6
pillow>=10.0,<12
pyarrow>=14.0,<18
